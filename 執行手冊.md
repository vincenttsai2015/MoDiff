# MoDiff 執行手冊

在晶創26（`nano4.nchc.org.tw`）上跑 MoDiff。

## 分工

seed 分 0 / 1 / 2 三份，以下把 `<S>` 換成自己的 seed 編號。

config 與腳本都在 repo 裡，不要自己改——要調整的話改 repo 再讓大家 pull。

---

## A. 環境

晶創26 與台灣杉二號的 `/home` 不共用，環境要重建。`--account` 是 `acd109125`。

```bash
ssh <帳號>@nano4.nchc.org.tw
```

```bash
cd ~ && wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh && bash Miniconda3-latest-Linux-x86_64.sh -b -p ~/miniconda3 && ~/miniconda3/bin/conda init bash && source ~/.bashrc
```

```bash
conda create -n modiff python=3.10 -y && conda activate modiff
```

torch 不能沿用台灣杉那套，H200 是 sm_90：

```bash
PIP_USER=0 pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
```

```bash
PIP_USER=0 pip install easydict matplotlib networkx numpy==1.26.0 nvidia_ml_py3 pandas pyemd PyYAML rdkit scikit_learn scipy sympy tqdm eden_kernel
```

`PIP_USER=0` 防止套件落到 `~/.local` 蓋過 conda env。

### 驗證

要在運算節點跑，登入節點禁止計算：

```bash
srun --account=acd109125 --partition=dev --nodes=1 --gres=gpu:1 --cpus-per-task=12 --time=00:10:00 ~/miniconda3/envs/modiff/bin/python -c "import torch; print(torch.__version__, torch.cuda.get_device_name(0), torch.cuda.get_device_capability(0))"
```

**要看到**：**NVIDIA H200** 與 **(9, 0)**。

---

## B. 程式碼

```bash
cd ~ && git clone https://github.com/vincenttsai2015/MoDiff.git && cd MoDiff && mkdir -p logs/slurm data/macro
```

### 驗證

```bash
cd ~/MoDiff && ls run_modiff_nano4.sh check_modiff.sh to_digraph.py eval_all_metrics.py run_eval_nano4.sh && ls config/Macro/*.yaml | wc -l && grep -c "_next" config/Macro/Macro_wiki_vote_burst_support.yaml && grep -c "MODIFF_SEQ_LEN" run_modiff_nano4.sh
```

**要看到**：五個檔案都在、config **32** 個、最後兩個數字不是 0。

```bash
cd ~/MoDiff && grep -c "MODIFF_RUN_TAG" main.py && grep -c "_eigh_safe" utils/data_loader.py && grep -c "n_use = min" utils/graph_utils.py && grep -c "sched_getaffinity" evaluation/mmd.py && grep -c "cuSOLVER" solver.py
```

**要看到**：`1 2 3 1 1`。

---

## C. 資料

安裝 rclone：

```bash
curl -O https://downloads.rclone.org/rclone-current-linux-amd64.zip && unzip -q rclone-current-linux-amd64.zip && mkdir -p ~/bin && cp rclone-*/rclone ~/bin/ && chmod +x ~/bin/rclone && echo 'export PATH="$HOME/bin:$PATH"' >> ~/.bashrc && export PATH="$HOME/bin:$PATH"
```

`rclone.conf` 放到 `~/.config/rclone/rclone.conf` 並 `chmod 600`。

```bash
rclone listremotes
```

**要看到**：`gdrive:`。

下載並解開（1.6 GB，解開後 19 GB）：

```bash
cd ~ && rclone copy --drive-root-folder-id 1wBCissrBmxsVubagKFIvzQoRR83snwLP gdrive:modiff_data_new.tar.gz . -P && tar xzf modiff_data_new.tar.gz -C MoDiff/data/macro
```

訓練讀長檔名、取樣讀短檔名，用符號連結補上：

```bash
cd ~/MoDiff/data/macro && for d in */; do b=${d%/}; for tag in V R T; do ln -sf "macro_${b}${tag}.pkl" "${b}/${tag}.pkl"; done; done
```

### 驗證

```bash
cd ~/MoDiff && ls -d data/macro/*/ | wc -l && du -sh data/macro && python -c "
import json
m=json.load(open('data/macro/wiki_vote_burst_support/meta.json',encoding='utf-8'))
print(f\"T={m['T']} 序列={m['n_sequences']} 圖數={m['n_graphs']}\")
print('三份張數:',m['split_sizes'],' 等長:',len(set(m['split_sizes'].values()))==1)
print('file2:',m['file2'])"
```

**要看到**：**40** 個目錄、約 **14 GB**、`T=16`、序列 **188**、
三份張數相同、`file2` 結尾是 **`_next`**。

```bash
cd ~/MoDiff && python -c "
import pickle, numpy as np
d='data/macro/wiki_vote_burst_support'
g=pickle.load(open(f'{d}/macro_wiki_vote_burst_supportV.pkl','rb'))
e=np.array([x.number_of_edges() for x in g]).reshape(-1,15).mean(axis=0)
print('每張平均邊數 t=16..30:', ' '.join(f'{v:.0f}' for v in e))"
```

**要看到**：邊數在中段隆起後回落，不是單調遞減。

```bash
cd ~/MoDiff && ls data/macro/wiki_vote_burst_support/ | wc -l && ~/miniconda3/envs/modiff/bin/python -c "
import pickle
g=pickle.load(open('data/macro/wiki_vote_burst_support/macro_wiki_vote_burst_supportV.pkl','rb'))
print(len(g), type(g[0]).__name__, g[0])"
```

**要看到**：**11** 個項目（10 個 pkl + meta.json，其中三個是符號連結）、
型別是 **DiGraph**。

是 `Graph` 的話跑一次轉換（符號連結會自動跳過）：

```bash
cd ~/MoDiff && nohup ~/miniconda3/envs/modiff/bin/python -u to_digraph.py > /tmp/todi.log 2>&1 &
```

---

## D. 先跑一組

```bash
cd ~/MoDiff && sbatch --account=acd109125 --partition=dev --time=04:00:00 --job-name=MoDiff_macro_wiki_vote_burst_support_<S> run_modiff_nano4.sh macro_wiki_vote_burst_support <S>
```

```bash
squeue --me
```

跑完：

```bash
tail -40 ~/MoDiff/logs/slurm/modiff_*.out
```

**三件事都要對**：

1. `[eigh] N/M 張退到 CPU 雙精度，0/M 張分解失敗`——分解失敗數要是 **0**
2. epoch 摘要行的 `train adj` 是有限值，不是 `nan`
3. 最後有 `Sampling Done`

出現一整排 `nan~!!!` 回 B 段驗證。其他錯誤帶著 log 回報。

### 驗證形狀

```bash
cd ~/MoDiff && python -c "
import pickle,glob,numpy as np
p=glob.glob('$HOME/modiff_generated/*wiki_vote_burst_support*/MoDiff/sampled_ts.pkl')[0]
g=pickle.load(open(p,'rb'))
e=[x.number_of_edges() for s in g for x in s]
print(f'{len(g)} 條 x {len(g[0])} 張，節點 {g[0][0].number_of_nodes()}')
print(f'每張邊數 平均 {np.mean(e):.1f}，空圖 {100*sum(1 for x in e if x==0)/len(e):.1f}%')"
```

**要看到**：每條 **15 張**。是 32 張的話 `git pull` 之後只重新取樣：

```bash
cd ~/MoDiff && sbatch --account=acd109125 --partition=dev --time=04:00:00 run_modiff_nano4.sh macro_wiki_vote_burst_support <S> eval
```

---

## E. 全部送出

```bash
cd ~/MoDiff && SEEDS="<S>" bash check_modiff.sh
```

**要看到**：組合都以 `_<S>` 結尾、總數 32；開頭的「辨識出執行中的組合：N 個」
要跟 `squeue --me | wc -l` 對得上。

```bash
SEEDS="<S>" bash check_modiff.sh --submit
```

送不完的等 queue 空再執行同一條。只有 checkpoint 沒有取樣紀錄的會用 `eval`
模式補，不重訓。

---

## F. 看進度

```bash
cd ~/MoDiff && for d in data/macro/*/; do b=$(basename "$d"); r="MoDiff_macro_${b}_<S>"; s="-"; ls "logs_sample/${b}/${r}/"*.log >/dev/null 2>&1 && grep -lq "Sampling Done" "logs_sample/${b}/${r}/"*.log 2>/dev/null && s="完成"; [ "$s" = "-" ] && [ -f "checkpoints/${b}/${r}_comp.pth" ] && s="只缺取樣"; printf '%-42s %s\n' "$b" "$s"; done
```

```bash
ls ~/modiff_generated/*_<S>/MoDiff/sampled_ts.pkl 2>/dev/null | wc -l
```

**要看到**：**32**。

---

## G. 打包回傳

生成的圖一定要帶回來，指標由 `eval_all_metrics.py` 事後統一算。

```bash
cd ~ && tar czf modiff_seed<S>.tar.gz modiff_generated/*_<S>/ && du -sh modiff_seed<S>.tar.gz
```

```bash
cd ~ && rclone copy modiff_seed<S>.tar.gz --drive-root-folder-id 1Njcr1dG_4Zr6v7yNjTTPLSqPKSgMs1Wo gdrive: -P
```

---

## H. 算十項指標

五個模型一起算，所以三個 repo 的產出都要在這台上。缺席的模型會被跳過。

```bash
cd ~/MoDiff && sbatch run_eval_nano4.sh <S>
```

```bash
squeue --me && tail -5 ~/MoDiff/logs/eval10_*.out
```

**要看到**：`--- <組合> / <模型> (300 vs 300) ---` 一組一組往下跑。

跑完：

```bash
tail -3 ~/MoDiff/logs/eval10_*.out && wc -l < ~/eval_seed<S>.csv
```

**要看到**：`===== DONE`，五個模型都齊時是 **161** 列（32 組 x 5 + 標題）。

```bash
cd ~ && rclone copy eval_seed<S>.csv --drive-root-folder-id 1Njcr1dG_4Zr6v7yNjTTPLSqPKSgMs1Wo gdrive: -P
```

### 某個模型當時還沒跑完

算的時候還在寫的檔案會讀失敗、那一列缺席，不會產生錯的數字。

```bash
grep -c "失敗" ~/MoDiff/logs/eval10_*.out
ls ~/gm_generated/*/GraphMaker/sampled_ts.pkl 2>/dev/null | wc -l
grep -c ",GraphMaker," ~/eval_seed<S>.csv
```

**要看到**：`失敗` 是 0，後兩個數字相同。不相同就補算那個模型，
其他四個不會重算：

```bash
cd ~/MoDiff && MODELS=GraphMaker sbatch --export=ALL run_eval_nano4.sh <S>
```

寫到 `~/eval_seed<S>_GraphMaker.csv`，不覆蓋原本那份。

上傳失敗顯示 `invalid_grant: maybe token expired` 的話，是 OAuth token 過期，
要在本機 `rclone config reconnect gdrive:` 之後把 `rclone.conf` 重新傳過來。
