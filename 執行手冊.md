# MoDiff 執行手冊

在晶創26（`nano4.nchc.org.tw`）上跑 MoDiff，資料是注入 burst / hysteresis
巨觀動態的多層時序圖序列。

## 分工

一人負責一個 seed，互不干擾。

| seed | 負責人 |
|---|---|
| 0 | 奕諾 |
| 1 | 永結 |
| 2 | 嘉憲 |

以下把 `<S>` 換成自己的 seed 編號。

config 與腳本都在 repo 裡，`git pull` 就會拿到最新的。
**不要自己改**——要調整的話改 repo 再讓大家 pull，這樣三個 seed 的設定才一致。

queue 名額是各帳號獨立的 20 個，腳本預設就是 20，不用調。

---

## A. 環境（只做一次）

晶創26 與台灣杉二號是**獨立叢集**，`/home` 不共用，環境要重建一次。
帳號沿用 iService 的，`--account` 是 `acd109125`。

```bash
ssh <帳號>@nano4.nchc.org.tw
```

沒有 conda module，要自己裝：

```bash
cd ~ && wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh && bash Miniconda3-latest-Linux-x86_64.sh -b -p ~/miniconda3 && ~/miniconda3/bin/conda init bash && source ~/.bashrc
```

```bash
conda create -n modiff python=3.10 -y && conda activate modiff
```

**torch 不能沿用台灣杉那套。** H200 是 Hopper（sm_90），舊版 torch 的 binary
不含 sm_90 的 kernel，搬過去會報 `no kernel image is available`。

```bash
PIP_USER=0 pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
```

```bash
PIP_USER=0 pip install easydict matplotlib networkx numpy==1.26.0 nvidia_ml_py3 pandas pyemd PyYAML rdkit scikit_learn scipy sympy tqdm eden_kernel
```

`PIP_USER=0` 防止套件落到 `~/.local`——那裡的東西會蓋過 conda env。

### 驗證

**要在運算節點跑**，登入節點禁止計算：

```bash
srun --account=acd109125 --partition=dev --nodes=1 --gres=gpu:1 --cpus-per-task=12 --time=00:10:00 ~/miniconda3/envs/modiff/bin/python -c "import torch; print(torch.__version__, torch.cuda.get_device_name(0), torch.cuda.get_device_capability(0))"
```

**要看到**：**NVIDIA H200** 與 **(9, 0)**。capability 不是 9.0 就是 torch 版本不對。

## B. 取得程式碼

```bash
cd ~ && git clone --depth 1 https://github.com/vincenttsai2015/MoDiff.git && cd MoDiff && mkdir -p logs/slurm data/macro
```

### 驗證

```bash
cd ~/MoDiff && ls run_modiff_nano4.sh check_modiff.sh to_digraph.py && ls config/Macro/*.yaml | wc -l
```

**要看到**：三個腳本都在，config 數量是 **32**。

```bash
cd ~/MoDiff && grep -c "MODIFF_RUN_TAG" main.py && grep -c "_eigh_safe" utils/data_loader.py && grep -c "n_use = min" utils/graph_utils.py && grep -c "sched_getaffinity" evaluation/mmd.py && grep -c "cuSOLVER" solver.py
```

**要看到**：`1 2 3 1 1`。有 0 的話程式碼版本不對，`git pull` 之後再確認一次。

## C. 取得資料

資料是 5.3 GB，不在 repo 裡（GitHub 單檔上限 100 MB，我們最大的檔 185 MB）。
向奕諾取得後放到 `~/MoDiff/data/macro/`。

### 補上短檔名

**訓練與取樣讀的檔名不同**，兩種都要在：

| 階段 | 讀的檔名 |
|---|---|
| 訓練 | `macro_<組合>R.pkl` / `V.pkl` / `T.pkl` |
| 取樣 | `R.pkl` / `V.pkl` / `T.pkl` |

拿到的資料只有長檔名，短檔名用符號連結補，不佔額外空間：

```bash
cd ~/MoDiff/data/macro && for d in */; do b=${d%/}; for tag in R V T; do ln -sf "macro_${b}${tag}.pkl" "${b}/${tag}.pkl"; done; done
```

### 驗證

```bash
cd ~/MoDiff && ls data/macro | wc -l && du -sh data/macro && ls -la data/macro/wiki_vote_burst_support/
```

**要看到**：**32** 個目錄、約 **5.3 GB**，而且最後一條列出
`meta.json`、`macro_wiki_vote_burst_support{,R,V,T}.pkl` 四個檔，
加上 `R.pkl` / `V.pkl` / `T.pkl` 三條符號連結。

```bash
cd ~/MoDiff && ~/miniconda3/envs/modiff/bin/python -c "
import pickle
g=pickle.load(open('data/macro/wiki_vote_burst_support/macro_wiki_vote_burst_supportR.pkl','rb'))
print(len(g), type(g[0]).__name__, g[0])"
```

**要看到**：型別是 **DiGraph**。是 `Graph` 的話跑一次轉換
（128 個檔案，32 組 × 4 個 pkl；符號連結會自動跳過）：

```bash
cd ~/MoDiff && nohup ~/miniconda3/envs/modiff/bin/python -u to_digraph.py > /tmp/todi.log 2>&1 &
```

要讀寫 5.3 GB，會跑一段時間。看進度：

```bash
tail -f /tmp/todi.log
```

---

## D. 先跑一組

確認整條流程通得了再送全部。

```bash
cd ~/MoDiff && sbatch --account=acd109125 --partition=dev --time=04:00:00 --job-name=MoDiff_macro_wiki_vote_burst_support_<S> run_modiff_nano4.sh macro_wiki_vote_burst_support <S>
```

```bash
squeue --me
```

跑完後：

```bash
tail -40 ~/MoDiff/logs/slurm/modiff_*.out
```

**三件事都要對**：

1. `[eigh] N/M 張退到 CPU 雙精度，0/M 張分解失敗`——**分解失敗數要是 0**
2. epoch 摘要行的 `train adj` 是有限值，**不是 `nan`**
3. 最後有 `Final Metrics: {...}`

出現一整排 `nan~!!!` 就是版本不對，回 B 段驗證。
其他錯誤把 log 給奕諾，程式碼要改的話會 push 到同一個分支，
之後 `git pull` 取得。

## E. 全部送出

```bash
cd ~/MoDiff && SEEDS="<S>" bash check_modiff.sh
```

**要看到**：清單裡的組合都以 `_<S>` 結尾，總數 32。
開頭那行「辨識出執行中的組合：N 個」要跟 `squeue --me | wc -l` 對得上。

```bash
SEEDS="<S>" bash check_modiff.sh --submit
```

送不完的等 queue 空再執行同一條。

只有 checkpoint 沒有取樣紀錄的會自動用 `eval` 模式補，不重訓。

## F. 看進度

```bash
cd ~/MoDiff && for d in data/macro/*/; do b=$(basename "$d"); r="MoDiff_macro_${b}_<S>"; s="-"; ls "logs_sample/${b}/${r}/"*.log >/dev/null 2>&1 && grep -lq "Final Metrics" "logs_sample/${b}/${r}/"*.log 2>/dev/null && s="完成"; [ "$s" = "-" ] && [ -f "checkpoints/${b}/${r}_comp.pth" ] && s="只缺取樣"; printf '%-42s %s\n' "$b" "$s"; done
```

## G. 收集指標

MoDiff 的 MMD / KS 隨生成一起產出，不用另外算。

```bash
cd ~/MoDiff && grep -h "Final Metrics" logs_sample/*/MoDiff_*_<S>/*.log 2>/dev/null | sort -u | wc -l
```

**要看到**：**32**。

```bash
cd ~/MoDiff && for d in data/macro/*/; do b=$(basename "$d"); m=$(grep -h "Final Metrics" logs_sample/${b}/MoDiff_macro_${b}_<S>/*.log 2>/dev/null | tail -1); [ -n "$m" ] && echo "${b}|${m#Final Metrics: }"; done > ~/modiff_metrics_seed<S>.txt && wc -l < ~/modiff_metrics_seed<S>.txt
```

**要看到**：**32** 行。

## H. 打包回傳

```bash
cd ~ && tar czf modiff_seed<S>.tar.gz modiff_metrics_seed<S>.txt MoDiff/logs_sample/*/MoDiff_*_<S>/ && du -sh modiff_seed<S>.tar.gz
```

從自己的電腦抓：

```bash
scp <帳號>@nano4.nchc.org.tw:~/modiff_seed<S>.tar.gz .
```

---

## 這個 repo 的幾個特性

**每次執行會複製一份 config**：`config/Macro/Macro_<seed>_<組合>.yaml`，
只改 `seed` 與 `name` 兩行。那些是執行時產生的，不要提交，
`check_modiff.sh` 也會跳過它們。

**`max_node_num` 與 `spec_dim` 由 `set_data_dims.py` 依實測資料填**，
資料換了一定要重跑那一步。

**`V` / `R` / `T` 不是 train / val / test。** `dataloader_TD_train_comp` 把這三個檔
當成 `transform_type = [1, 2, 3]` 的三個角度，逐一取同一個索引組成 Hermitian 矩陣。
上游自己的前處理也是這樣寫的。副作用是迭代長度取決於最短的那個 list，
實際只用到約八分之一的訓練資料——這是已知的待處理項目，先照現狀跑通拿基準。
